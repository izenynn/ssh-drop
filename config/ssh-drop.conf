# ssh-drop configuration

port = 7022
host_key = key/id_ed25519
authorized_keys = key/authorized_keys
auth_method = publickey

# auth_timeout = 30

# log_level = info
# log_file =

# Secret source (exactly one must be set)
secret_file = secret/secret
# secret = my-secret-value
# secret_env = SSH_DROP_SECRET

# Optional username check (at most one)
# auth_user = admin
# auth_user_file = secret/user
# auth_user_env = SSH_DROP_USER

# Password source (required when auth_method is password or both)
# auth_password = my-password
# auth_password_file = secret/password
# auth_password_env = SSH_DROP_PASSWORD
